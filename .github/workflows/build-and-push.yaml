name: ci

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  bake:
    name: Build/push images/Compose files
    runs-on: ubuntu-latest
    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: cloud
          endpoint: ${{ secrets.DOCKER_BUILDER_ENDPOINT }}

      - name: Set tag for builds
        run: |
          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then
            echo "IMAGE_TAG=dev" >> $GITHUB_ENV
          else
            TAG="${GITHUB_REF#refs/tags/}"
            echo "IMAGE_TAG=${TAG}" >> $GITHUB_ENV
          fi

      - name: Build and push tagged image
        uses: docker/bake-action@v6
        with:
          push: true

      # Skipping this as there is a bug with Compose that prompts for confirmation when volumes are in the
      # Compose file, even with the `-y` flag.
      # - name: Publish Compose files
      #   run: |
      #     export CONTENT_PATH="/placeholder"
      #     if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then
      #       docker compose -f compose.run.yaml publish ${DOCKERHUB_USERNAME}/labspace:dev --with-env -y
      #       docker compose -f compose.content-dev.yaml publish ${DOCKERHUB_USERNAME}/labspace-content-dev:dev --with-env -y
      #     else
      #       TAG="${GITHUB_REF#refs/tags/}"
      #       docker compose -f compose.run.yaml publish ${DOCKERHUB_USERNAME}/labspace:latest --with-env -y
      #       docker compose -f compose.run.yaml publish ${DOCKERHUB_USERNAME}/labspace:${TAG} --with-env -y
      #       docker compose -f compose.content-dev.yaml publish ${DOCKERHUB_USERNAME}/labspace-content-dev:latest --with-env -y
      #       docker compose -f compose.content-dev.yaml publish ${DOCKERHUB_USERNAME}/labspace-content-dev:${TAG} --with-env -y
      #     fi
